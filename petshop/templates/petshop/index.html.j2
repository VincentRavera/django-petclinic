{% if owner_list %}
    <h1>Petshop:</h1>
    <h3>Select a client:</h3>
    <ul>
    {% for owner in owner_list %}
        <li><a href="{% url 'petshop:owner' owner.id %}">{{ owner.name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No Customes are available.</p>
{% endif %}
